# Todo List (Flask)

Simple todo list web application built with Flask. Intended as a small demo / learning project — not hardened for production by default.

## Features
- Create, edit, complete, and delete todos
- Server-side persistence using SQLite (default)
- Minimal HTML templates + CSS for UI
- Single-file start: `app.py`

## Quick start (local)
1. Create and activate a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # macOS / Linux
   .venv\Scripts\activate      # Windows (PowerShell)

